# QUICKSTART - 外付け日本語キーボードで Ctrl と CapsLock を交換

このガイドでは、**外付けの日本語キーボード（JIS配列）で Ctrl キーと CapsLock キーを交換する**ユースケースを、ステップバイステップで説明します。

## ユースケース

多くのプログラマーやエディタユーザーは、**CapsLock キーを Ctrl キーとして使いたい**という要望を持っています。

- **内蔵キーボード**: そのまま使用（設定なし）
- **外付けキーボード（HHKB など）**: Ctrl と CapsLock を交換

このツールを使えば、**複数のキーボードで異なる設定を同時に適用**できます。

## 前提条件

- Windows 10 / 11（64-bit）
- .NET 8.0 以上がインストール済み
- 外付けキーボード（USB接続）が接続されている
- キーボードが Windows で正しく認識されている

## ステップ1: キーボードを検出

まず、接続されているキーボードを確認します。

```bash
> list
```

**出力例**:
```
Found 2 keyboard(s):
 [0] HHKB Professional (VID: 04FE, PID: 0021)
 Handle: 18350
 ID: 04FE:0021
 
 [1] Built-in Keyboard (VID: 0000, PID: 0000)
 Handle: 18351
 ID: 0000:0000
```

外付けキーボードの **デバイスID（例：`04FE:0021`）** をメモしておきます。

## ステップ2: デバイスを設定

外付けキーボードを設定対象として指定します。

```bash
> config 04FE:0021
```

**出力例**:
```
Configuring device: HHKB Professional (VID: 04FE, PID: 0021)
Device configuration saved.
```

## ステップ3: Ctrl と CapsLock を交換

**スワップ（交換）** コマンドを使用して、Ctrl と CapsLock を入れ替えます。

```bash
> map 04FE:0021 CapsLock LCtrl swap
```

**出力例**:
```
Added mapping: CapsLock <-> LCtrl (swap)
```

## ステップ4: 設定を確認

設定が正しく追加されたか確認します。

```bash
> show 04FE:0021
```

**出力例**:
```
Device: HHKB Professional (VID: 04FE, PID: 0021)
Mappings:
 - CapsLock <-> LCtrl (swap)
```

## ステップ5: 設定を保存

設定をファイルに保存して、次回起動時に自動的に読み込まれるようにします。

```bash
> save
```

**出力例**:
```
Configuration saved to: C:\Users\YourName\AppData\Local\KeyboardRemapper\config.json
```

## ステップ6: 設定をテスト

実際にキーマッピングが機能しているか、テストします。

```bash
> test 04FE:0021 CapsLock
```

**出力例**:
```
Testing mapping for CapsLock on device 04FE:0021
Press CapsLock key on the device...
Key press detected: CapsLock
Mapped to: LCtrl
```

外付けキーボードの **CapsLock キーを押してみて**、Ctrl キーとして動作することを確認します。

## ステップ7: 設定を読み込み

アプリケーションを再起動した後、保存した設定を読み込みます。

```bash
> load
```

**出力例**:
```
Configuration loaded from: C:\Users\YourName\AppData\Local\KeyboardRemapper\config.json
```

これで、外付けキーボードで Ctrl + CapsLock の交換が有効になります。

---

## 設定ファイルの内容確認

設定ファイルは以下の場所に保存されます:

```
C:\Users\<ユーザー名>\AppData\Local\KeyboardRemapper\config.json
```

**ファイル内容例**:
```json
{
  "version": "1.0",
  "devices": [
    {
      "device_id": "04FE:0021",
      "device_name": "HHKB Professional",
      "vid": "04FE",
      "pid": "0021",
      "enabled": true,
      "mappings": [
        {
          "type": "swap",
          "source_key": "CapsLock",
          "target_key": "LCtrl",
          "description": "CapsLock <-> LCtrl"
        }
      ]
    },
    {
      "device_id": "0000:0000",
      "device_name": "Built-in Keyboard",
      "vid": "0000",
      "pid": "0000",
      "enabled": false,
      "mappings": []
    }
  ]
}
```

---

## トラブルシューティング

### Q: キーボードが検出されない

**A**: 以下を確認してください:

1. キーボードが USB で正しく接続されているか確認
2. デバイスマネージャーでキーボードが認識されているか確認
3. `detect` コマンドで再検出を試みる:
   ```bash
   > detect
   ```

### Q: マッピングが反映されない

**A**: 以下を確認してください:

1. 設定が正しく保存されているか確認:
   ```bash
   > show 04FE:0021
   ```
2. アプリケーションを再起動して設定を再読み込み:
   ```bash
   > load
   ```
3. 設定ファイルが破損していないか確認:
   ```
   C:\Users\<ユーザー名>\AppData\Local\KeyboardRemapper\config.json
   ```

### Q: 特定のキーが認識されない

**A**: サポートされているキー名を確認してください。キー名は**大文字小文字を区別**します:

- `CapsLock` ✅ (正しい)
- `capslock` ❌ (間違い)
- `CAPSLOCK` ❌ (間違い)

---

## 応用例

### 複数のキーボードで異なる設定

**内蔵キーボード**: 設定なし（デフォルト）  
**外付けキーボード1**: CapsLock → LCtrl  
**外付けキーボード2**: CapsLock → Escape

```bash
# 外付けキーボード1
> config 04FE:0021
> map 04FE:0021 CapsLock LCtrl swap

# 外付けキーボード2
> config 1234:5678
> map 1234:5678 CapsLock Escape remap

> save
```

### キーマッピングを削除

特定のキーマッピングを削除したい場合:

```bash
> remove 04FE:0021 CapsLock
```

### デバイスの全マッピングをクリア

デバイスのすべてのマッピングをクリアしたい場合:

```bash
> clear 04FE:0021
```

---

## 次のステップ

- [README.md](README.md): 詳細なドキュメント
- [BUILD.md](BUILD.md): ビルド方法
- [CONTRIBUTING.md](CONTRIBUTING.md): 貢献ガイドライン

---

**Happy Remapping! 🎹**
